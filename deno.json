{
  "name": "@discere-os/openblas.wasm",
  "version": "0.3.28",
  "description": "High-performance OpenBLAS linear algebra library with SIMD optimizations and TypeScript-first API",
  "exports": {
    ".": "./src/lib/index.ts",
    "./types": "./src/lib/types.ts",
    "./simd": "./build/openblas-simd.js",
    "./fallback": "./build/openblas.js"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.14",
    "@std/testing": "jsr:@std/testing@^1.0.0",
    "module": "data:application/javascript,export const createRequire = () => (id) => { if (id === 'path') return { dirname: () => '', join: (...args) => args.join('/') }; if (id === 'url') return { fileURLToPath: (url) => url.replace('file://', '') }; if (id === 'fs') return { readFileSync: () => { throw new Error('fs not supported'); } }; throw new Error(`Module ${id} not found`); }; export default { createRequire };",
    "path": "data:application/javascript,export const dirname = () => ''; export const join = (...args) => args.join('/'); export default { dirname, join };",
    "url": "data:application/javascript,export const fileURLToPath = (url) => url.replace('file://', ''); export default { fileURLToPath };",
    "fs": "data:application/javascript,export const readFileSync = () => { throw new Error('fs not supported'); }; export default { readFileSync };"
  },
  "tasks": {
    "demo": "deno run --allow-read --allow-write demo-deno.ts",
    "demo:matrices": "deno run --allow-read --allow-write demo-matrices.ts",
    "demo:ml": "deno run --allow-read --allow-write demo-ml.ts",
    "test": "deno test --allow-read --allow-write --no-check tests/deno/",
    "test:basic": "deno test --allow-read --allow-write --no-check tests/deno/basic.test.ts",
    "test:simd": "deno test --allow-read --allow-write --no-check tests/deno/simd.test.ts",
    "test:performance": "deno test --allow-read --allow-write --no-check tests/deno/performance.test.ts",
    "benchmark": "deno run --allow-read --allow-write bench/blas.bench.ts",
    "benchmark:gemm": "deno run --allow-read --allow-write bench/gemm.bench.ts",
    "benchmark:vectors": "deno run --allow-read --allow-write bench/vectors.bench.ts",
    "build": "deno task build:wasm",
    "build:wasm": "./scripts/build-all.sh",
    "build:simd": "./scripts/build-simd.sh",
    "build:fallback": "./scripts/build-fallback.sh",
    "build:npm": "deno run --allow-all _build_npm.ts",
    "build:all": "deno task build:wasm && deno task build:npm",
    "publish:npm": "deno task build:all && cd npm && npm publish",
    "publish:dry": "deno task build:all && cd npm && npm publish --dry-run",
    "clean": "rm -rf build/ dist/ npm/ && echo '✅ Cleaned all build artifacts'",
    "check": "deno check src/lib/index.ts",
    "check:all": "deno check src/lib/index.ts && deno check demo-deno.ts && deno check bench/blas.bench.ts && deno check _build_npm.ts"
  },
  "compilerOptions": {
    "lib": ["deno.ns", "dom", "es2022", "deno.unstable"],
    "strict": true,
    "allowUnreachableCode": false,
    "allowUnusedLabels": false,
    "exactOptionalPropertyTypes": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noPropertyAccessFromIndexSignature": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  },
  "fmt": {
    "include": ["src/", "tests/", "demo*.ts", "bench/", "scripts/"],
    "exclude": ["dist/", "build/", "npm/"]
  },
  "lint": {
    "include": ["src/", "tests/", "demo*.ts", "bench/", "scripts/"],
    "exclude": ["dist/", "build/", "npm/"]
  },
  "test": {
    "include": ["tests/deno/"],
    "exclude": ["tests/unit/"]
  }
}